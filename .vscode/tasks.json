{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Windows Build",
            "command": "${workspaceFolder}/Emulate.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}